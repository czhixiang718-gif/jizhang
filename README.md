# 海南万企回款管理系统 (WanQi Collection Management System)

这是一个基于 Node.js + Express + React + SQLite 的全栈回款管理系统。

## 📋 环境要求
- Node.js (建议 v14.0.0 或更高版本)
- npm (通常随 Node.js 安装)

## 🚀 快速开始

### 1. 启动后端服务 (Backend)
后端服务负责处理 API 请求和数据库操作。

```bash
# 进入项目根目录
cd jizhang

# 安装依赖
npm install

# 启动服务 (运行在 http://localhost:3001)
npm run dev
```

### 2. 启动前端界面 (Frontend)
前端界面是用户交互的网页应用。

```bash
# 进入前端目录 (新开一个终端窗口)
cd client

# 安装依赖
npm install

# 启动开发服务器 (通常运行在 http://localhost:5173)
npm run dev
```

## 🔌 端口说明
- **前端 (Frontend)**: `http://localhost:5173` (访问此地址使用系统)
- **后端 (Backend)**: `http://localhost:3001` (API 服务)

## 🛠️ 技术栈
- **前端**: React, Vite, Tailwind CSS, Recharts, Lucide React
- **后端**: Express.js
- **数据库**: SQLite3 (本地文件存储 `database.sqlite`)

## ✨ 主要功能
- 项目录入与管理
- 资金回款追踪
- 自动/手动数据备份
- 财务报表导出 (Excel/JSON)
- 可视化数据分析 (图表)
- 管理员账号安全设置
